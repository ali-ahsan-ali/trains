# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Trains' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Trains
  pod 'Alamofire'
  pod 'SwiftLint'
  pod 'SwiftFormat/CLI', '~> 0.49'
  pod 'SSZipArchive'

  target 'TrainsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TrainsUITests' do
    # Pods for testing
  end

end
